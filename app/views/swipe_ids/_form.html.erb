<%= form_with(model: swipe_id, local: true) do |form| %>
  <% if swipe_id.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(swipe_id.errors.count, "error") %> prohibited this swipe_id from being saved:</h2>

      <ul>
      <% swipe_id.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.text_field :student_id, id: :swipe_id_student_id %>
  </div>

  <div class="field">
    <%= form.label :time %>
    <%= form.datetime_select :time, id: :swipe_id_time %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
