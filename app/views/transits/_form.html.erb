<%= form_with(model: transit, local: true) do |form| %>
  <% if transit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(transit.errors.count, "error") %> prohibited this transit from being saved:</h2>

      <ul>
      <% transit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :transit_name %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :transit_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
