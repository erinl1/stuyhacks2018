<p id="notice"><%= notice %></p>

<p>
 <strong>Studentcard:</strong>
 <%= @swipe.studentcard %>
</p>

<p>
 <strong>Time:</strong>
 <%= @swipe.time %>
</p>
<p>
 <strong>Status:</strong>
 <% student = Student.find_by(student_uuid: @swipe.studentcard.student.student_uuid) %>
 <% lines=student.lines%>
 <% last_metro_swipe=student.rides.last.time%>
 <%= student.name %>
 <%= student.rides %>
 <%= student.rides.last %>
  1<%= student.rides.last.attributes %>
2  <% student.rides.last.attributes.each do |attr_name,attr_value|%>
    <%=attr_name%>:<%=attr_value%>
   <%end%>
 <%= lines %>
<%= last_metro_swipe %>



</p>

<%= link_to 'Edit', edit_swipe_path(@swipe) %> |
<%= link_to 'Back', swipes_path %>
